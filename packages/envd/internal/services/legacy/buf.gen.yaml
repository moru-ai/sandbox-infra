version: v1
plugins:
  - plugin: go
    out: .
    opt: paths=source_relative

managed:
  enabled: true
  optimize_for: SPEED
  go_package_prefix:
    default: github.com/moru-ai/sandbox-infra/packages/envd/internal/services/legacy
